# Cargo workspace 根配置
# 编译目标和链接参数在此处配置，对 kernel/ crate 生效
# tests/ crate 通过 make test --target x86_64-unknown-linux-gnu 覆盖此目标

[build]
# 默认编译目标：ARMv8 裸机，无操作系统、无标准库
target = "aarch64-unknown-none"

[target.aarch64-unknown-none]
# 链接脚本路径由 Makefile 通过 RUSTFLAGS 环境变量传入，
# 根据 PLATFORM 选择 kernel/linker-qemu.lds 或 kernel/linker-d2000.lds
# 此处不设置 link-arg，避免与 Makefile 传入的值冲突

# 使用 LLVM 的 lld 链接器（比系统 ld 更好地支持裸机目标）
linker = "rust-lld"
