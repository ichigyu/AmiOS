[package]
name = "loader"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "loader"
path = "src/main.rs"

[dependencies]
# UEFI 协议封装：提供类型安全的 SimpleFileSystem、BootServices 等接口
uefi = { version = "0.33" }
